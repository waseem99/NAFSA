{
  "name": "nicl-presentation-deploy-shell",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "prototype"
  ],
  "scripts": {
    "build": "npm --prefix prototype run build",
    "start": "npm --prefix prototype run start"
  },
  "engines": {
    "node": "20.x"
  }
}
